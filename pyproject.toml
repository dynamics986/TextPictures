[project]
name = "textpictures"
version = "0.1.0"
description = "Generate high-resolution Chinese-character density mosaics from images."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.0.0",
    "opencv-python>=4.10.0",
    "Pillow>=10.0.0",
]
